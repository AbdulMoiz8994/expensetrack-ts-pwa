import React from 'react'

export const History = () => {
   
    // const {transactions} = initialValue

    return (
        <div>
            <ul>
            {/* {transactions.map((transactionObj) =>{
               return(
              <li key={transactionObj.id}>{transactionObj.desc} <span> {transactionObj.value}</span></li>
               )
            })} */}
            </ul>
        </div>
    )
}
